# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/knowyourcampus?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Prince@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8081
server.servlet.context-path=/api

# JWT Configuration
jwt.secret=KnowYourCampus2026SecretKeyForJWTTokenGenerationAndValidation256Bit
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=http://localhost:5173,http://localhost:3000

# Application Configuration
spring.application.name=knowyourcampus-backend

# Logging
logging.level.com.knowyourcampus=DEBUG
logging.level.org.springframework.security=DEBUG
