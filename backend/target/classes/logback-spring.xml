<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- Local Development: Console Logging -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Production: CloudWatch Logging -->
    <springProfile name="prod">
        <appender name="CLOUDWATCH" class="ca.pjer.logback.AwsLogsAppender">
            <layout>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </layout>
            <logGroupName>KnowYourCampus-Logs</logGroupName>
            <logStreamName>Backend-Stream</logStreamName>
            <logRegion>ap-south-1</logRegion>
            <maxBatchLogEvents>50</maxBatchLogEvents>
            <maxFlushTimeMillis>5000</maxFlushTimeMillis>
            <maxBlockTimeMillis>5000</maxBlockTimeMillis>
            <retentionTimeDays>7</retentionTimeDays>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="CLOUDWATCH"/>
        </root>
    </springProfile>
</configuration>
